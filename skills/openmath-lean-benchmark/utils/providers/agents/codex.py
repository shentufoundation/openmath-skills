"""Codex agent stub. Replace with real CLI when available."""


def build_command(prompt: str, filepath: str, **kwargs: object) -> list[str]:
    """Build argv for codex (placeholder). Customize when CLI spec is known."""
    return [
        "codex", "-p", prompt,
    ]
